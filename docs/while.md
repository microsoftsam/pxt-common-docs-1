# While

Repeat code..


```block
while(true) {
}
```

The while loop has ...


```blocks
input.onButtonPressed(Button.A, () => {
    let index = 4;
    while(index >= 0) {
        led.plot(index, index);
        index--;
    }
})
```
